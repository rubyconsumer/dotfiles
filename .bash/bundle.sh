alias   bx="bundle exec"
alias  tbe="time bundle exec"
alias ttbe="time RAILS_ENV=test bundle exec"

alias   bi="bundle install"

alias   bu="bundle update"   # update the current environment
alias   bo="bundle open"     # open source directory of the given bundled gem
alias   bs="bundle show"     # shows all gems that are part of the bundle, or the path to a given gem
alias bviz="bundle viz"      # generates a visual dependency graph

